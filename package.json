{
  "name": "feedback-ops-copilot",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Mock Feedback Ops Copilot — classify inbox feedback, draft Notion upserts, never send without a human gate. Not Verde.",
  "scripts": {
    "test": "node --test scripts/test.mjs",
    "run:sample": "node scripts/run-sample.mjs",
    "smoke": "bash scripts/smoke-demo.sh",
    "dry": "node scripts/dry-test.mjs"
  },
  "engines": {
    "node": ">=18"
  },
  "license": "UNLICENSED"
}
